# version numbers, testing against two versions of node
node_js:
  - 0.12.3

env:
  - multi='xunit=shippable/testresults/result.xml spec=-'

before_script:
 - mkdir -p shippable/testresults
 - npm install

script:
  - ./node_modules/mocha/bin/_mocha -R mocha-multi
